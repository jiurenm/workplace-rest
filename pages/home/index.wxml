<view class="page">
  <view class="page-title">{{title}}</view>
  <view class="page-note">{{note}}</view>
  <countdown-ring task="{{tasks[0]}}" />
  <task-summary stats="{{stats}}" />
  <action-guide-sheet visible="{{false}}" />
</view>
